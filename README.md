# Particle Simulator

2D Particle: simulator + editor

## Quickstart

```bash
# Create an empty folder to run
mkdir local
cd local

# Run the editor
cargo run --release

# Run the simulator
... TODO ...
```
